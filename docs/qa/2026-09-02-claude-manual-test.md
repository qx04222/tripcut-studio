# Claude 手工级测试轮(v0.3.0-alpha.8,2026-09-02 夜)

方式:computer use(真实安装包 + cliclick + AX + 截图),以真实用户姿态逐页操作。

## P0 已修
| # | 发现 | 证据 | 修法 |
|---|---|---|---|
| 1 | **O 键打不上出点**:I 生效 O 无效,精选段无法保存(核心工作流断) | 实拍 mt2-02/04:I 已设 04.821,O 一直 "—";segments 表 0 行 | 全部剪辑快捷键改物理键 code 判定(KeyI/O/S/J/K/L、Digit),中文输入法下不再被吞;筛片页 F/X/1-5/L/R 同样处理 |
| 2 | **播放器控制条文字重叠**(业主同步反馈) | 实拍:时间码/入出点/文件名/帧号互相压字 | player-transport 固定列宽(142px/226px)改 auto 自适应;两个响应式断点同改;控制条改 min-height |

## P1 已修
| # | 发现 | 修法 |
|---|---|---|
| 3 | 素材封面出现纯黑帧(25% 位置恰逢黑场) | ffmpeg `thumbnail=90` 滤镜自动选代表帧 |
| 4 | L1 证据抽屉 Esc 关不掉、关闭 × 被右边缘裁切 | 加 Esc 监听;heading sticky + 按钮固定不压缩 |
| 5 | VFR 用红色 warning 徽章,像"素材有问题"(实为属性) | 改中性徽章 + 悬停解释"时间映射已处理,可正常使用" |
| 6 | 已关注文件夹时,SOURCE 仍写"尚未选择素材文件夹"(自相矛盾) | 有关注时显示"已关注 N 个素材文件夹" |
| 7 | 播放器技术黑话"帧 0 · 精确定位待采样"对剪辑师无意义 | 空态不显示;有值时"第 N 帧 · 精确定位 8 毫秒" |

## P2 记录(未修,待排期)
- 导入表格路径列中段省略,难辨同名不同目录素材(建议显示末两级目录)。
- 侧栏收起态下 AX 树无文字标签(自动化与读屏可达性)。
- 缩略图列表滚动到底部有一段空白区(虚拟滚动余量)。

## 举一反三:字号提级后的固定尺寸隐患复扫
- 全项目 CSS 复扫 `font-size: *px` = 0 残留(含独立 PlayerOverlay.css)。
- 固定列宽 grid 逐个复核:播放器三处已改自适应;其余均为 minmax/auto 或图标列(不含长文本),无重叠风险。
