# R25 play 车道 —— Q-1 / Q-3 + 业主 0.11.5 报告 TC-0115-001/002/004/005/006(2026-09-22)

- 分支 `feat/r25-playback`(worktree `~/Projects/tripcut-wt-r25-playback`),基线 main `4861f11`(v0.11.5,schema 54);已并入 `r25/integrate`(`cfdc412`:照片网格 + 预览画质,schema 55)。
- 分工:Codex(gpt-6-astra)两份任务书写代码;车道负责人(Claude)审 diff、迁移断言、先红后绿、门禁、真机、提交。
- 范围中途变更(接线人按业主 0.11.5 报告):原 Q-2「开在最精彩处」**作废并撤回**;新增 TC-0115-001/002/004/005/006。拍板真值见 scratch `r25-owner-report.md`。

## 1. 提交

| 提交 | 内容 |
|---|---|
| `3b7aa7b` | Q-3 修剪落地自动继续前按新出点重挂围栏(F-R24-04) |
| `7f9dc2a` → `72400f1` | Q-2 开在最精彩处 → **已撤回**(业主推翻) |
| `b60c71f` | Q-1 跨素材换源时旧画面保留到新实例首帧(F-R24-02) |
| `a294648` | TC-0115-001/002/004/005/006 统一播放语义(五项文件交织,合一个提交) |
| `dc4073d` | 真机发现:「镜头带连播」不受监视器「连播」开关管 |
| `ab4c30b` | merge `r25/integrate`(冲突两处,两边语义都保留,见 §4) |
| `6a23d9c` | 重拍 24 张预览基线(逐张看过,全是本轮预期变化) |
| 本报告 | docs |

## 2. 逐项处置与证据

真机装置沿用 R24 §4:颜色编码时间的两条 25 fps 夹具(clipA 全蓝;clipB 0–2.4 s 品红 / 2.4–12 绿 / 12 s 后黄,封面取在黄区),井中心取色 + ScreenCaptureKit 60 fps 亮度序列(蓝 78 / 黄 214 / 绿 112);隔离 profile、按 pid 激活、`TRIPCUT_LOG=debug` 的 `player command` 行。「改前」= R24 QA 包 `7187db6`(与 `4861f11` 的播放器/连播/监视器文件逐字相同,`git diff --stat` 为空)。证据在本会话 scratch `r25/ev/`(不入库)。

### Q-1(F-R24-02)跨素材切段露出井底封面 —— 完成
- 做法:视图层交接。`PlayerManager::open` 不再同步关旧会话,旧会话挪进 `outgoing`;新 NSOpenGLView 插在旧 view **之下**;新 worker 在 `PlaybackRestart` 之后第一次成功画帧交换时退役旧会话(发 Shutdown、进 orphans、不阻塞)。兜底:1500 ms 看门狗、新 worker 出错/退出(Drop)、open 失败、close、下一次 open;代次号隔离迟到交接;遮挡同时作用于 outgoing;命令/状态/viewport 只打当前会话。逻辑在新文件 `player/handoff.rs`(7 条单测)。
- **真机前后(下一段 A→B,封面帧时长)**:改前 **52 / 87 / 53 ms**(亮度序列 蓝→黄→绿);改后 `b60c71f` **0 / 0 / 0 ms**,最终包 `dc4073d` 8 次、合并包 `ab4c30b` 媒体池连切 3 次全部 **蓝→绿直接接帧**,相邻帧间隔 16–22 ms,品红(错帧)0。
- 先红后绿:`handoff.rs` 是新模块,基线无此接口不能直接回放;红的证据是上面的真机改前数字。

### Q-2(F-R24-03)—— 撤回
按业主 TC-0115-002「打开不同素材 → 0 → 自动播放」作废。改前测到的「先见第 0 帧再跳」为 135–157 ms 品红(3 次);现在不再跳(见 TC-0115-002)。

### Q-3(F-R24-04)修剪落地自动继续时重挂出点围栏 —— 完成
- `usePlaythrough` 段列表变更:挂起中的修剪落地 → 先 `await setEnd(新 out)`、令牌/段列表仍有效才 `play()`;失败走 `fail` 停连播;未挂起但当前段出点变了只重挂不 play;其他段变化不重挂。
- 先红后绿:4 条新测试在 `4861f11` 源码上 **4 红**,修后 17/17。
- 真机:改前(R24 `ev/trim1`)修剪后直接 `Play`,无 `SetEnd`;改后(最终包)`… SetEnd{None} → SetEnd{Some(51.9)} → Play`,之后接 2/3、3/3 段直到结束。

### TC-0115-002 素材模式:不同素材 → 0 → 自动播放 —— 完成
删掉就绪暂停与晚到「最精彩处」seek;设置页移除「选中素材从最精彩处开播」(旧键保留不读,不迁移);素材模式不设围栏;快切以最后选中为准。

| 验收标准 | 结果 |
|---|---|
| 初始位置 00:00.0 ± 0.10 s | ✅ 真机 A/B/A/B 四次切换 +1.0 s 时 1.00–1.24 s、+2.5 s 时 2.56–2.64 s(改前:A 停 55.0、B 停 2.5,暂停) |
| 加载后自动播放 | ✅ 按钮均为「暂停」、时码在走 |
| 不继承上一条 currentTime | ✅ 真机 + `MonitorR17Playfix` A=12.3→B=0 |
| 不停在建议 / 入点 / 缓存位置 | ✅ 改前停在建议 1 的 55.0 / 2.5;改后 0 起 |
| 连续切 10 条 | ✅ 自动化(`MonitorR17Playfix` 十次打开);真机夹具只有 2–3 条素材,切了 4+3 次 |
| 快切以最后选中为准 | ✅ 自动化(`PlayerOverlayR17` A→B→C);真机未专门快切 |
| 长短 / 带不带建议 | ✅ 自动化;真机 60 s / 50 s / 20 s(4K)三条 |
| 不改数据 | ✅ 断言源对象不变;真机未见写库 |
| 按钮 / 时码 / 画面一致 | ✅ 真机按钮=暂停、时码递增、画面亮度序列连续 |

### TC-0115-001 选段模式:无论连播开关都在出点停 —— 完成
新增 `playthrough/selection.ts` 活动选段真值;镜头带点镜块/刻度、检查器复播、镜头带连播统一进 `usePlaythrough`;先 `await SetEnd(out)` 再 `Play`;监视器连播关 → 出点停住保持暂停,开 → 进下一镜头;停在出点按播放 = 从入点重播;段内 seek 保留围栏,段外 seek 退出选段并暂停;媒体池选素材退出选段。

| 验收标准 | 结果 |
|---|---|
| 从入点播放并在出点 ±0.10 s 暂停 | ✅ 真机 镜头 3(2.52–10.52)停 **10.48**(出点前最后一帧,差 0.04 s);镜头 2(4.48–12.48)停 **12.44**;4K 镜头 4(0–8)停 **7.96**。改前镜头 3 放到 **21.64** 还在走 |
| 出点后无未选帧/音频 | ✅ 画面:停住后时码 5 s 不动;音频未单独录音验证(围栏是 mpv `end`,音画同停) |
| 连播关不进下一镜头 | ✅ 真机 3 次 |
| 连播开进下一镜头 | ✅ 真机 镜头 2 → 镜头 3 跨素材,`SetEnd` 在 `Play` 前 |
| 三章各两个镜头 | ⚠️ 自动化 30/50/60 fps 三章六镜;真机夹具只有 1 章 4 镜(待业主用真库抽查) |
| 同素材不连续段不播空隙 | ✅ 自动化(§8A 等);真机 镜头 1→2(同素材 46.48→4.48)直接 seek |
| 不改数据 | ✅ |
| UI 与数据一致 | ✅ 选段红框 = 段入出点、I/O 栏 = 段入出点、「第 n/m 段」 |

### TC-0115-004 / 006 进度条 —— 完成
默认完整素材;两态文本控件「完整素材 / 放大片段」(冻结 AX 名「切换进度条范围」保留,AXCheckBox);`ui.player.scrubber_view` 存设置;连播也遵守视图模式;放大窗口装不下真实位置 → 该次显示自动退回完整视图(偏好仍是放大,回到窗口内再放大),指针不再 clamp;指针/时码/AX 同一次状态回读。

| 验收标准 | 结果 |
|---|---|
| 选段出点 ±0.10 s 停或接下一段 | ✅ 同 001 |
| 指针与 currentTime ≤ 1 帧 | ✅ 自动化同快照;真机截图指针与时码同刻 |
| 时码 / 画面 / 指针同一时刻 | ✅ 自动化 `snapshotR25`(30/50/60) |
| 越过显示范围自动扩展 / 跟随 | ✅ 真机:改前放大 2–10.5 视图、位置 33.04 时指针钉在右端;改后位置 14.72 自动显示完整 0–50、指针在 14 s 处(截图 `ev/tc-after/crop-zoom-over.png`、合并包 `ev/merge3`) |
| 切模式立即定位真实位置 | ✅ 自动化(切模式不 seek) |
| 暂停 / 拖动 / ±1 s / 恢复后同步 | ✅ 自动化;真机未逐项 |
| 30 / 50 / 60 fps | ⚠️ 自动化三档;真机只有 25 fps 夹具 |
| 连播开 / 关分别验证 | ✅ 自动化 + 真机 |
| 默认完整 0→总长、绿框 ≤1 帧、建议外位置正确 | ✅ 真机截图 |
| 局部 / 完整明确命名 | ✅ 文字「完整素材」「放大片段」(按钮上显示的是**当前偏好**;放大偏好下越界临时显示完整时文字仍是「放大片段」,悬停说明写了) |
| 切下一条建议不缩回 | ✅ 自动化 |
| 跨素材、跨重启保持 | ✅ 真机:设放大 → 菜单退出 → 重启,`settings.ui.player.scrubber_view=zoom`、控件 AXValue=1 |

### TC-0115-005 建议按入点升序 —— 完成
`suggestionRanges` 按 `(入点, 出点, −分数)` 排,编号与导航同序,不改数据;未做「按推荐度」切换(报告为「如提供」)。
- 真机:clipA 改前「建议 1/3」= 51–59(分数最高);改后 21.5 → 33 → 51。clipB 本来就是升序(2.5 → 12 → 21.5)。
- 标准 3(按推荐度切换)不适用;其余 ✅(自动化乱序输入 + 相邻/不相邻 + 两种时间基)。

### 真机新发现并已修:「镜头带连播」被监视器连播开关截停(`dc4073d`)
`a294648` 让所有选段会话到点都看监视器 `autoAdvance`(默认关),于是按「镜头带连播」只放完第 1 段就停(R22/R23 行为回归;Q-3 修剪恢复后也因此卡在 1/3)。改为:`start()`(镜头带连播)总是接下一段;只有单选镜头(`select()`)才按开关。新测试在改前源码上红。真机:连播开关关着按「镜头带连播」,1/3 → 2/3 → 3/3 → 停在 10.48。

## 3. 先红后绿汇总
- Q-3:4 红(基线源码)→ 17/17。
- Q-2(已撤回):当时 7 文件 17 红 → 绿(记录保留,代码已撤)。
- TC 五项:相关 11 个测试文件在改前源码上 **41 红 / 38 绿** → 全绿;另迁移 `PhotoMonitorR21`、`ShotBandR13`(原「广播 seek-ratio」→「选段请求落点秒」)。
- `dc4073d`:1 条新测试改前红。
- 断言迁移清单见 Codex 报告(车道 scratch),都是「最精彩处 seek / 就绪暂停 / 片段视图默认」→ 新语义,没有删断言。

## 4. 合并 `r25/integrate`(`ab4c30b`)与叠加真机
- 冲突:`player/mod.rs`(Q-1 交接门与预览画质换源器同在 `run_worker` / `drain_events` / `PlaybackRestart`):两者都保留 —— `drain_events` 同时带 switcher 与 handoff gate;每轮先 `switcher.tick` 再 `handoff.redraw_ready`;`PlaybackRestart` 先开交接门再交给换源器。`Monitor.tsx` 导入行(选段真值 + 来源角标)。
- 合并后门禁:tsc / eslint 0 错 / vitest 276 文件 1913 绿 / cargo test 1394 绿 / clippy `-D warnings` 绿;fast-gates 见 §5。
- QA 包 `旅剪工作台_0.11.5_r25pb-qa-ab4c30b_qa_aarch64.dmg`(SHA-256 `eac29d7d051a6f1af124a72eac6dc06d5450c0a7d27a2fb58fd8cf3327a81410`),自动档:
  1. **媒体池连切 3 条**(A→B→A):都从 0 自动播(+1 s 在 1.08–1.24 s)、角标「原片 720p」(夹具 720p 无代理,角标正确);亮度序列 蓝→品红→绿→蓝,**无封面帧**。另加一条 4K 夹具(20 s,生成 540p 代理):播放「代理 540p」→ 暂停「原片 2160p · 暂停看原片」(换源 227 ms,误差 0.00 s)→ 再播回代理(32 ms,误差 0.04 s)。
  2. **选段模式**:连播关 镜头 3 停 10.48,暂停中角标「原片 720p · 暂停看原片」,再按播放从入点重播、再停 10.48;连播开 镜头 2 → 镜头 3。**4K 镜头 4(0–8 s,真换源)**:播放用代理,到出点停 7.96 → 暂停换原片(830 ms,误差 0)→ 位置仍 7.96、围栏未破 → 按播放换回代理(23 ms)从入点 0 重播 → 再停 7.96。
  3. **放大片段越出视口**:clipB 放大 2.5–10.5,播到 13.8 s 自动显示完整素材,指针在 13.8 s 处不钳边。

## 5. 门禁
- 合并前树 `dc4073d`:`fast-gates` **29/29 PASS**(运行目录 `qa/runs/2026-09-23T00-53-08Z-fast-gates`)。
- 合并后树 `6a23d9c`:`fast-gates` **29/29 PASS**(`qa/runs/2026-09-23T01-34-02Z-fast-gates`),preview-diff 重拍后仅 `34-move-to-episode` 一张 2.5%,单独重拍两次与基线 0.000% —— 截图时机抖动,不是回归;perf-bench total 65.4%、rss 92.6%。
- preview-diff(report-only)合并后 24 张超 0.5%:逐张 diffmap + 目检,全是本轮预期变化(完整素材刻度与文字控件、选段模式下的段条/红框/I/O 填段、热力气泡随刻度移动),已在 `6a23d9c` 单独重拍。注意:合并时 LFS 滤镜关着,照片车道 41–45 号基线落成了指针,`git lfs checkout qa/preview-baseline` 后才比得了。

## 6. 发现 / 留下轮

| 编号 | 级别 | 发现 | 处置 |
|---|---|---|---|
| F-R25-01 | **P1(0.11.5 已在线上)** | 连播中按「下一段」发出的 `Pause` 偶发卡 ~2 s:mpv `set_pause_state → ao_set_paused → ao_drain` 等音频放完(`sample` 栈为证);超过 2 s 命令超时时监视器出「播放器异常 · 播放器命令响应超时」,有一次卡死在「播放器尚未打开」。**基线 0.11.5 同样复现**(加锁 A/B:基线 3/4、本分支 4/4,同一份 profile);只在第 1 段出点被修剪成 51.9(Q-3 真机修剪后)的 profile 上出现,出点 53.1 的 profile 上 0/9。触发条件是 mpv 判音频已到 EOF(`audio_status == STATUS_EOF`)时暂停,为什么离出点 3.5 s 就到 EOF 没查清 | 未修。下轮:给 Pause 路径加「音频 EOF 时先 `ao-reset`/不 drain」或把命令超时与 UI 报错解耦;先用 `--log-file` 复现拿 mpv 日志 |
| F-R25-02 | P3 | 单选一个镜头(连播关)时,监视器上方仍显示「连播 · 第 n/m 段 · 上一段 / 下一段 / 停止 / 循环」,镜头带按钮变「停止连播」—— 选段模式复用了连播状态机,文案说的是「连播」 | 记录。建议 `select()` 会话显示「选段 · 第 n/m 段」、带按钮不变;要动 AX 名,留给接线人定 |
| F-R25-03 | P3 | 点镜块**最左 10 px(修剪把手)**不进选段模式,只按修剪跟随 seek 定位(素材模式、无围栏) | 记录。探针第一次就是点在这里误判;业主手点到把手的概率不高 |
| F-R25-04 | P3 | 修剪拖动中每次跟随 seek 都额外发一条 `Pause`(一次拖 30 多条 IPC) | 记录,无可见影响 |
| F-R25-05 | P3 | 4K 暂停换原片 830 ms(夹具 h264 ultrafast),超过 TC-0115-003「≤0.5 s」 | 转 quality 车道 |
| F-R25-06 | 流程 | 两组 A/B(ab-fix、ab-q1play,00:43–00:47 UTC)是在**没拿到真机锁**时跑的:等锁循环超时后命令被转后台继续跑,当时另一条车道可能在用屏幕/音频 | 结论只采用之后**加锁重跑**的那组(lk-base / lk-final);F-R25-01 的结论以加锁数据为准 |

## 7. 拍板记录(无人值守)
1. Q-1 选视图层交接(优先项),不用入点帧替代封面。
2. 同一素材全屏来回重开实例也按「0 + 自动播放」(不恢复旧位置)。
3. 放大模式越界 → 该次显示退回完整素材,偏好保留放大;回到窗口内再放大。
4. 「镜头带连播」(显式整条放)不受监视器连播开关管;开关只管单选镜头 / 检查器复播到点后接不接。
5. 段外人工 seek 退出选段并暂停(不自动播);段内 seek 保留围栏。
6. TC-0115-005 不做「按推荐度」切换(报告为「如提供」)。
7. 五项 TC 合成一个提交(同一走带 / 同一测试文件交织,硬拆会出现中间不可编译的提交)。

## 8. 待业主
1. **F-R25-01**(0.11.5 已有):连播中按下一段偶发 2 s 卡顿 / 「播放器异常」,本轮查到 mpv 音频 drain,未修。
2. F-R25-02:单选镜头时显示「连播 · 第 n/m 段」「停止连播」的文案要不要改成「选段」。
3. TC-0115-001「三章各两个镜头」、004「30/50/60 fps」需要用真库素材抽查(夹具只有 25 fps、1 章)。
4. 「选中素材从最精彩处开播」设置项已移除(业主拍板打开即从 0 播)。
