# v0.2.0 发布记录（2026-09-02 07:00）

## 判定
硬门 Benchmark 第五跑双证:runner exit 0 + metrics PASS **18/18**。
- critical_recall = 1.0(发布门禁)
- vfr_mapping 6/6(max_error 33367µs,= manifest 29.97fps 检查点与 24fps 素材帧栅格错位,evaluator 容差内)
- proxy_mapping 69/69,max_error 0µs
- narrative_critical_suppression_rate = 0
- source_tree_unchanged = true(原片零变化)
- 语义门(scene/important_event)仍为 informational,待真实素材标注后恢复硬门

## 产物
- `~/Desktop/旅剪工作台_0.2.0_aarch64.dmg`(SHA256 f333f854cdd139a324a11d265dc071515a312af58ac33c7a0f9f13b49ad218e5,ad-hoc 签名未公证)
- DMG 冒烟:挂载 ok、codesign --deep ok、版本 0.2.0

## 本轮修复链(v0.1→v0.2)
1. benchmark prepare() 从未把 vfr/proxy checkpoints 写入 request.json → driver 观测全空(修 driver_fixture_request)
2. VFR 采样对 4K HEVC 全量 packet dump 超 30s 超时 → 改 `-read_intervals %+#600` 限量
3. 采样失败被当导入失败(违反 Asset Safety,9 条素材 blocked、压制率 0.05)→ 改为降级:警告+空采样表,照常导入
4. s4 夹具数据缺陷(非产品 bug):4 个 vfr_* 变体是 cfr 版字节拷贝(MD5 相同,被产品指纹去重正确合并)、kassel_058/toledo_065 moov 损坏、F-012 用 36s 素材配 300s 期望 → 重生成 9 个夹具(select 丢帧+fps_mode vfr 产真 VFR、305s loop 长素材),seed-map 已更新

## 教训
Benchmark 失败先验尸夹具字节(md5 全库查重、ffprobe 直读)再怀疑产品;本轮三层假象全部来自测试底座与数据,产品侧唯一真缺陷是第 3 条。

---

# v0.2.2 追加(2026-09-02 11:25,业主报障后的实测修复轮)

业主装 0.2.0 后报:工具链已装仍提示未安装、恢复页无法滚动无法进入。真机实测(打包版 DMG 安装+cliclick+截图)确诊并修复四项:

1. **P0 工具链误报**:Finder 启动的 .app 只有系统最小 PATH,settings 与 doctor 各自的解析器都找不到 /opt/homebrew/bin;两处统一为同一解析器并追加 Homebrew/MacPorts 目录。真机复测:「ffmpeg、ffprobe 与 whisper-cli 均可探测」。
2. **P0 恢复页死锁**:body 全局禁滚+恢复页超一屏→「进入工作台」不可达;恢复页改自带滚动区。真机复测:可滚到底、按钮可点。
3. **P0 正式版误用开发库**:development_root() 无条件用 dev/(97条测试夹具+测试进程的异常退出标记全被正式版读到);release 改用独立 default/。真机复测:全新库直进工作台,dev 库零触碰。
4. **P0 正常退出不清哨兵**:清理代码在 run() 之后,macOS 退出走 process::exit 永不执行→每次启动都是恢复页;移入 RunEvent::Exit。真机复测:quit 后 .unclean-exit 消失,重启直进工作台。

附带:徽标去掉开发期 P1/P2 术语(FLOW 素材工作流/CONFIG 系统设置)。
验证环:安装→首启(残留标记出恢复页)→滚动→进入→退出(标记清除)→重启(直进)全绿;六门(tsc/vitest 71/cargo test/clippy/build/DMG冒烟)全绿。
产物:桌面 旅剪工作台_0.2.2_aarch64.dmg(SHA256 739849f4…)。

---

# v0.2.3 手工级全测轮(2026-09-02 下午,业主指令「全部程序操作实测」)

打包版 DMG 安装后 cliclick+AX+截图逐链路走查:启动→恢复→设置逐分区→导入(NSOpenPanel 真选目录,10条)→筛片(评级/F收藏/八维/Stack/Best Take)→故事板→交付(全产物落盘+ffprobe验证)→播放器。

## 修复三个 P0
1. **设置页白屏"闪退"**(业主报障:改预算即闪退):五处在 setState updater 闭包里读 React 合成事件(currentTarget 异步阶段为 null)——预算框敲键即抛 TypeError 整树卸载成白屏。dev 模式抓到崩溃栈实锤。全修+根级 ErrorBoundary(渲染错误显示中文错误页可返回,不再白屏)。
2. **L3 provider 全报"PATH 未找到"**(业主报障):第三份 PATH 解析器(llm.rs)+缺用户级目录。统一 settings 解析器,追加 ~/.local/bin(claude)/~/.kimi-code/bin(kimi)。真机:三 CLI 全绿。
3. **播放器区域更新死锁**(全测发现,打包版播放器从未可用):player_set_viewport 同步命令占 AppKit 主线程等 worker 回复,worker 的 resize_surface 又 run_on_main_thread 等主线程——环形等待2秒超时。探针版逐段定位(libmpv/render context 全正常,卡在 Resize)。改 async+spawn_blocking。真机:视频画面渲染、播放推进、Esc 退出全通。

## 附带
- 双实例白屏:第二实例现弹「检测到另一个实例,本窗口只读」明确提示。
- 全链真机通过:10条导入(去重/blocked零)、评级收藏落库、交付包(精选/粗剪51s=预估/CSV含BOM八维/SRT/说明)双验证。

## 待观察(不阻塞)
- 播放中时间码截图瞬间仍显示 00:00:00(画面已推进)——待业主人工确认是否复现。
- 语义门恢复、L3 实调、双主题目测:仍留业主人工轮。

产物:桌面 旅剪工作台_0.2.3_aarch64.dmg。

---

# v0.2.4 筛片页逐控件轮(2026-09-02 下午,业主报障:Stack 有些收不起+堆积)

确诊:quality_exempt(人物/信息)Stack 被强制永远展开且点击无效(只有视觉 Stack 可切换)——「有些能收有些不能」的根源;多个强制展开的绝对定位浮层互相覆盖并盖住下行——「堆积看不清」的根源。
修复:全部 Stack 统一点击/Tab 切换展开收起(同时仅一个),徽章加 ▸/▾ 指示,浮层 z-index 抬升。dev 实拍双向验证。

逐控件补测(dev 实拍):Stack 展开/收起双向、成员条锁定按钮 active、过滤 tab(全部/收藏/未评)、八维筛选下拉(八项全)、排除废片开关、只看 Stack 首选开关、深色主题(设置/筛片/交付/导入四页无白块)、界面缩放 130% 生效——F5 双主题真机目测遗留项就此关闭。
产物:桌面 旅剪工作台_0.2.4_aarch64.dmg。
