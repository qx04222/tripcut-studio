# 任务卡 P5-F3:中文帮助系统 + 筛片墙布局打磨(P2 债)

状态:排队(F2 后)。实施:Codex。主审:Claude。

## 范围
- 帮助:设置页「帮助」分区或独立浮层——快捷键总表(自动从常量生成)/五步工作流图文(导入→筛片→打点→故事→交付)/常见问题(工具链缺失/剪映版本/加密说明)全中文;关于页补开源许可清单(从 Cargo.toml+package.json 生成静态清单,构建脚本)
- 筛片墙打磨(业主观感债):卡片墙从单列改真网格(自适应列数 minmax(280px,1fr));头部信息条重排(LIBRARY行/过滤chips/提示分三行变一行工具栏);容器卡「N连拍」样式统一;空态文案;两主题目测过关
- 全局细节:焦点环统一 token;按钮 hover 态;列表滚动条样式;窗口标题随视图变化
## 验收
前端 ≥4 测试;五门全绿;真机两主题截图逐页目测(主审)。

## 纪律
纯写不碰 git 不跑构建;纯前端+脚本,不碰 Rust core 逻辑(许可清单脚本除外)。
