# 任务卡 P5-F5:设置页与全局排列重优化(业主指令+调研落地)

状态:排队(最终 GO 后派发)。实施:Codex。主审:Claude(双主题真机逐项目测)。
依据:docs/research/2026-09-01-macos-settings-ui-research.md(HIG 数值+高星应用共性)。业主原话:"设置一起里面的大小排列都得重新优化"。

## 三步序(调研 5.5,严格按序,每步可独立验收)
1. **行高与间距 token 统一**(不改结构不改文案):落地调研 5.3 token 表(--space-page-margin:20px/--space-section-gap:24px/--space-row-gap:8px/行高44-52px 统一/--font-size-section-title:13px/body:14px/caption:12px/行高比1.5-1.6/分隔线上下12px);全设置页行采用调研 5.4 统一行结构:[图标20-24px][主标题+灰色副标题左对齐]…弹性…[控件右对齐];Select 宽度≤40%
2. **分区收敛**(Ice 经验:减法优先):审查现有 8 分区,合并冗余项;说明文字直接展示不藏 tooltip;危险操作(清缓存重建)独立成行+警示色+分隔线隔离,移至所在分区最底
3. **侧栏导航结构**:设置页改「左侧分类侧栏+右侧单列滚动」(System Settings 13+ 范式);分区排序按调研 5.2:外观/性能高频在上→工具链/分析中段→关于倒二→危险区最底;侧栏行高一致、选中圆角高亮
- 全局连带:筛片/导入/交付页的行高与字阶同步采用新 token(只换 token 值不动布局);中文数字 `font-variant-numeric: tabular-nums`
## 验收
前端 ≥5 测试(行结构组件/侧栏路由/危险区位置断言);五门全绿;主审双主题逐分区截图目测+1512 与全屏两档;业主终验。
## 纪律
纯写不碰 git 不跑构建;只动前端与 token;不改任何设置语义与后端。
